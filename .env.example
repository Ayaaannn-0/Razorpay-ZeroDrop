# Groq AI API (Optional: if omitted, ZeroDrop uses its built-in deterministic rule engine)
GROQ_API_KEY=gsk_your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Application Secrets (Pre-configured with zero-config defaults for local evaluation)
# SECRET_KEY=whisperer-dev-secret-key-change-in-prod-9921
# FLASK_ENV=development

# Optional: Production Webhook & Database Overrides
# RAZORPAY_WEBHOOK_SECRET=whsec_local_sandbox_dev_secret_token
# DATABASE_URL=sqlite:///payment_whisperer.db

